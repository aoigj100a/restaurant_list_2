<div class="row my-5">
    <div class="col-md-6 m-auto">
        <div class="card card-body">
            <h1 class="text-center mt-3">註冊</h1>
            {{> message}}
            <form class="form-horizontal" action='/user/register' method="POST">
                <fieldset id="tab021">
                    <div class="control-group">
                        <!-- Username -->
                        <label class="control-label" for="name">用戶名</label>
                        <div class="controls">
                            <input type="text" id="name" name="name" placeholder="" class="form-control" value="{{name}}">
                            <p class="text-secondary">帳號可以包含任何字母或數字，不能有空格。</p>
                        </div>
                    </div>

                    <div class="control-group">
                        <!-- E-mail -->
                        <label class="control-label" for="email">E-mail</label>
                        <div class="controls">
                            <input type="text" id="email" name="email" placeholder="" class="form-control" value="{{email}}">
                            <p class="text-secondary">請確定此電子郵件為可使用狀態，以方便未來系統使用，如補寄會員密碼信。</p>
                        </div>
                    </div>

                    <div class="control-group">
                        <!-- Password-->
                        <label class="control-label" for="password">密碼</label>
                        <div class="controls">
                            <input type="password" id="password" name="password" placeholder="" class="form-control" value="{{password}}">
                            <p class="text-secondary">密碼至少應包含4個字符。</p>
                        </div>
                    </div>

                    <div class="control-group">
                        <!-- Password -->
                        <label class="control-label" for="confirmPassword">密碼 (再次確認)</label>
                        <div class="controls">
                            <input type="password" id="confirmPassword" name="confirmPassword" placeholder=""
                                class="form-control" value="{{confirmPassword}}">
                            <p class="text-secondary">請再次確認密碼。</p>
                        </div>
                    </div>
                    <button type="submit" class="btn btn-primary btn-block">Register</button>
                    <a href="/auth/facebook" class="btn btn-info btn-block">Facebook Login</a>
                </fieldset>

            </form>
            <p class="lead mt-4">
                已經有帳號了嗎? <a href="/user/login">登入</a>
            </p>
        </div>
    </div>
</div>